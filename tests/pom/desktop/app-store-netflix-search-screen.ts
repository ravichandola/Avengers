import { DesktopPage } from "../../../src/drivers/desktop/pom/desktop-page";
import { DesktopDriver } from "../../../src/drivers/desktop/desktop-driver";

/** Auto-generated (desktop — selectors = AX / System Events titles) */
export class AppStoreNetflixSearchScreen extends DesktopPage {
  readonly splitGroup = this.element("split group");
  readonly sidebar = this.element("Sidebar");
  readonly searchTextField = this.element("search text field");
  readonly search = this.element("Search");
  readonly cancel = this.element("cancel");
  readonly outlineRow = this.element("outline row");
  readonly discover = this.element("Discover");
  readonly arcade = this.element("Arcade");
  readonly create = this.element("Create");
  readonly work = this.element("Work");
  readonly play = this.element("Play");
  readonly develop = this.element("Develop");
  readonly categories = this.element("Categories");
  readonly updates = this.element("Updates");
  readonly column = this.element("column");
  readonly group = this.element("group");
  readonly field = this.element("Field");
  readonly raviChandola = this.element("Ravi Chandola");
  readonly resultsForNetflix = this.element("Results for “Netflix”");
  readonly macApps = this.element("Mac Apps");
  readonly iphoneIpadApps = this.element("iPhone & iPad Apps");
  readonly filters = this.element("Filters");
  readonly section = this.element("section");
  readonly amazonPrimeVideoOriginalsMoviesTvSportsFourAndThreeQuartersStars =
    this.element(
      "Amazon Prime Video, Originals, movies, TV, sports, four and three quarters stars",
    );
  readonly amazonPrimeVideo = this.element("Amazon Prime Video");
  readonly originalsMoviesTvSports = this.element(
    "Originals, movies, TV, sports",
  );
  readonly text = this.element("text");
  readonly reDownload = this.element("Re-download");
  readonly fourAndThreeQuartersStars = this.element(
    "four and three quarters stars",
  );
  readonly el99k = this.element("9.9K");
  readonly artwork = this.element("Artwork");
  readonly streamingForNetflixForNetflixPeacockAndMoreFourAndAHalfStars =
    this.element(
      "Streaming for Netflix, For Netflix, Peacock and more, four and a half stars",
    );
  readonly streamingForNetflix = this.element("Streaming for Netflix");
  readonly forNetflixPeacockAndMore = this.element(
    "For Netflix, Peacock and more",
  );
  readonly getInAppPurchases = this.element("Get, In-App Purchases");
  readonly fourAndAHalfStars = this.element("four and a half stars");
  readonly el372 = this.element("372");
  readonly whatsappMessengerSimpleReliablePrivateFourAndAHalfStars =
    this.element(
      "WhatsApp Messenger, Simple. Reliable. Private., four and a half stars",
    );
  readonly whatsappMessenger = this.element("WhatsApp Messenger");
  readonly simpleReliablePrivate = this.element("Simple. Reliable. Private.");
  readonly open = this.element("Open");
  readonly el83k = this.element("83K");
  readonly streamingForNetflixForNetflixYoutubeVideoFourStars = this.element(
    "Streaming+ for Netflix, For Netflix, YouTube video &.., four stars",
  );
  readonly streamingForNetflix3 = this.element("Streaming+ for Netflix");
  readonly forNetflixYoutubeVideo = this.element(
    "For Netflix, YouTube video &..",
  );
  readonly fourStars = this.element("four stars");
  readonly el154 = this.element("154");
  readonly toolbar = this.element("toolbar");
  readonly closeButton = this.element("close button");
  readonly fullScreenButton = this.element("full screen button");
  readonly minimiseButton = this.element("minimise button");

  constructor(driver: DesktopDriver) {
    super(driver);
  }
}
