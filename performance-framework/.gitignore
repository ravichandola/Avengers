# --- Generated by perf runs (framework CLI, examples, custom runners) ---
# JTL, JMX, index.html, report.json, allure-results/, …
perf-output/

# JMeter often writes this in the working directory (repo root also ignores *.log)
jmeter.log

# Optional: local JMeter tarball extract or tool cache kept next to the package
.tools/
apache-jmeter-*/

# npm / Vite (repo root already ignores node_modules/ and dist/ — listed for clarity)
node_modules/
dashboard/node_modules/
dashboard/dist/
